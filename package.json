{
  "name": "ownership-graph",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "cp docs/index.html docs/404.html && touch docs/.nojekyll",
    "preview": "vite preview"
  }
}
